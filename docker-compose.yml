version: '3.8'
services:
sentimentchat:
build:
context: .
dockerfile: Dockerfile
container_name: sentimentchat_app
env_file:
- .env
environment:
- CLAUDE_API_KEY=${CLAUDE_API_KEY}
platform: windows/amd64
volumes:
- ./SentimentChat/Data:/app/Data
restart: unless-stopped