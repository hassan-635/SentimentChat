@page
@model ThreeScreenModel
@{
    Layout = null;
    ViewData["Title"] = "Three Screen System";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Three Screen Sentiment Analysis System</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --bg-primary: #0a0a0a;
            --bg-secondary: #1a1a1a;
            --mobile-bg: #0f1419;
            --server-bg: #1a1a2e;
            --bot-bg: #2a0845;
            --accent: #6f42c1;
            --positive: #25d366;
            --negative: #dc3545;
            --neutral: #6c757d;
            --text-primary: #e9edef;
            --text-secondary: #8696a0;
            --border-color: #2a3942;
        }

        body {
            background: linear-gradient(135deg, #0a0a0a 0%, #2a0845 100%);
            color: var(--text-primary);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            height: 100vh;
            margin: 0;
            overflow: hidden;
        }

        .main-container {
            height: 100vh;
            display: flex;
            flex-direction: column;
            overflow: hidden;
        }

        nav, footer, .navbar, .footer, .header {
            display: none !important;
            height: 0 !important;
            min-height: 0 !important;
            padding: 0 !important;
            margin: 0 !important;
            border: none !important;
        }

        .screens-container {
            flex: 1;
            display: flex;
            gap: 0.6rem;
            padding: 0.5rem;
            overflow: hidden;
            min-height: 0;
        }

        .device {
            flex: 1;
            background: var(--bg-secondary);
            border: 2px solid var(--border-color);
            border-radius: 1rem;
            display: flex;
            flex-direction: column;
            position: relative;
            overflow: hidden;
            max-height: 58vh;
            min-height: 220px;
        }

        .device-header {
            padding: 0.5rem;
            border-bottom: 1px solid var(--border-color);
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .device-title {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-weight: bold;
        }

        .device-status {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 0.8rem;
        }

        .status-dot {
            width: 8px;
            height: 8px;
            border-radius: 50%;
            background: var(--positive);
            animation: pulse 2s infinite;
        }

        @@keyframes pulse {
            0% { opacity: 1; }
            50% { opacity: 0.5; }
            100% { opacity: 1; }
        }

        /* Mobile Device */
        .mobile {
            background: var(--mobile-bg);
        }

        .mobile-header {
            background: var(--bg-secondary);
            color: var(--positive);
        }

        .mobile-messages {
            flex: 1;
            overflow-y: auto;
            padding: 0.4rem;
            background: var(--mobile-bg);
            min-height: 60px;
            max-height: 160px;
        }

        .message {
            margin-bottom: 1rem;
            display: flex;
            align-items: flex-start;
            animation: fadeIn 0.3s ease-in;
        }

        @@keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .message.user {
            justify-content: flex-end;
        }

        .message.bot {
            justify-content: flex-start;
        }

        .message-bubble {
            max-width: 80%;
            padding: 0.75rem 1rem;
            border-radius: 1rem;
            position: relative;
            word-wrap: break-word;
        }

        .message.user .message-bubble {
            background: var(--positive);
            color: white;
            border-bottom-right-radius: 0.25rem;
        }

        .message.bot .message-bubble {
            background: var(--bg-secondary);
            color: var(--text-primary);
            border-bottom-left-radius: 0.25rem;
        }

        .message-info {
            font-size: 0.7rem;
            color: var(--text-secondary);
            margin-top: 0.25rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .sentiment-badge {
            padding: 0.2rem 0.4rem;
            border-radius: 1rem;
            font-size: 0.6rem;
            font-weight: 600;
        }

        .sentiment-positive { background: var(--positive); color: white; }
        .sentiment-negative { background: var(--negative); color: white; }
        .sentiment-neutral { background: var(--neutral); color: white; }

        .mobile-input {
            background: var(--bg-secondary);
            padding: 0.4rem;
            border-top: 1px solid var(--border-color);
            flex-shrink: 0;
            min-height: 45px;
            max-height: 55px;
        }

        .input-group {
            display: flex;
            gap: 0.5rem;
        }

        .form-control {
            background: var(--mobile-bg);
            border: 1px solid var(--border-color);
            color: var(--text-primary);
            border-radius: 2rem;
            padding: 0.5rem 0.75rem;
            font-size: 0.9rem;
            height: auto;
        }

        .form-control:focus {
            background: var(--mobile-bg);
            border-color: var(--positive);
            color: var(--text-primary);
            box-shadow: 0 0 0 0.2rem rgba(37, 211, 102, 0.25);
        }

        .btn-send {
            background: var(--positive);
            border: none;
            color: white;
            border-radius: 50%;
            width: 2rem;
            height: 2rem;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s ease;
            flex-shrink: 0;
        }

        .btn-send:hover {
            background: #128c7e;
            transform: scale(1.05);
        }

        /* Server Device */
        .server {
            background: var(--server-bg);
        }

        .server-header {
            background: var(--accent);
            color: white;
        }

        .server-content {
            flex: 1;
            display: flex;
            flex-direction: column;
            overflow: hidden;
        }

        .processing-log {
            flex: 1;
            overflow-y: auto;
            padding: 0.4rem;
            font-family: 'Courier New', monospace;
            font-size: 0.72rem;
            max-height: 160px;
        }

        .log-entry {
            margin-bottom: 0.5rem;
            padding: 0.5rem;
            border-radius: 0.25rem;
            border-left: 3px solid var(--accent);
            background: rgba(111, 66, 193, 0.1);
            animation: slideIn 0.3s ease-out;
        }

        @@keyframes slideIn {
            from { opacity: 0; transform: translateX(-20px); }
            to { opacity: 1; transform: translateX(0); }
        }

        .log-entry.processing {
            border-left-color: var(--accent);
            background: rgba(111, 66, 193, 0.1);
        }

        .log-entry.positive {
            border-left-color: var(--positive);
            background: rgba(37, 211, 102, 0.1);
        }

        .log-entry.negative {
            border-left-color: var(--negative);
            background: rgba(220, 53, 69, 0.1);
        }

        .log-entry.neutral {
            border-left-color: var(--neutral);
            background: rgba(108, 117, 125, 0.1);
        }

        .log-time {
            color: var(--text-secondary);
            font-size: 0.7rem;
        }

        .log-message {
            color: var(--text-primary);
            margin-top: 0.25rem;
        }

        .server-stats {
            background: rgba(111, 66, 193, 0.1);
            padding: 0.4rem 0.5rem;
            border-top: 1px solid var(--border-color);
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 0.3rem;
            flex-shrink: 0;
            max-height: 60px;
        }

        .stat-item {
            text-align: center;
            padding: 0.25rem;
            background: rgba(111, 66, 193, 0.05);
            border-radius: 0.25rem;
        }

        .stat-value {
            font-size: 0.95rem;
            font-weight: bold;
            color: var(--text-primary);
        }

        .stat-name {
            font-size: 0.5rem;
            color: var(--text-secondary);
            line-height: 1;
        }

        /* Bot Device */
        .bot {
            background: var(--bot-bg);
        }

        .bot-header {
            background: var(--accent);
            color: white;
        }

        .bot-content {
            flex: 1;
            display: flex;
            flex-direction: column;
            overflow: hidden;
        }

        .bot-responses {
            flex: 1;
            overflow-y: auto;
            padding: 0.4rem;
            max-height: 160px;
        }

        .response-item {
            margin-bottom: 1rem;
            padding: 1rem;
            border-radius: 0.5rem;
            border-left: 4px solid var(--accent);
            background: rgba(111, 66, 193, 0.1);
            animation: slideIn 0.5s ease-out;
        }

        .response-item.positive {
            border-left-color: var(--positive);
            background: rgba(37, 211, 102, 0.1);
        }

        .response-item.negative {
            border-left-color: var(--negative);
            background: rgba(220, 53, 69, 0.1);
        }

        .response-item.neutral {
            border-left-color: var(--neutral);
            background: rgba(108, 117, 125, 0.1);
        }

        .response-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 0.5rem;
        }

        .response-sentiment {
            padding: 0.25rem 0.5rem;
            border-radius: 1rem;
            font-size: 0.7rem;
            font-weight: bold;
            color: white;
        }

        .response-time {
            color: var(--text-secondary);
            font-size: 0.7rem;
        }

        .response-text {
            color: var(--text-primary);
            margin: 0.5rem 0;
            line-height: 1.3;
            font-size: 0.9rem;
        }

        .bot-thinking {
            display: none;
            align-items: center;
            gap: 0.5rem;
            padding: 1rem;
            background: rgba(111, 66, 193, 0.2);
            border-radius: 0.5rem;
            margin-bottom: 1rem;
        }

        .bot-thinking.active {
            display: flex;
        }

        .thinking-dots {
            display: flex;
            gap: 0.25rem;
        }

        .dot {
            width: 6px;
            height: 6px;
            border-radius: 50%;
            background: var(--accent);
            animation: bounce 1.4s infinite ease-in-out both;
        }

        .dot:nth-child(1) { animation-delay: -0.32s; }
        .dot:nth-child(2) { animation-delay: -0.16s; }

        @@keyframes bounce {
            0%, 80%, 100% { transform: scale(0); }
            40% { transform: scale(1); }
        }

        /* Responsive Design */
        @@media (max-width: 1400px) {
            .header h1 {
                font-size: 1.8rem;
            }
            
            .header p {
                font-size: 0.95rem;
            }
        }

        @@media (max-width: 1200px) {
            .screens-container {
                flex-direction: column;
                gap: 0.8rem;
                padding: 0.6rem;
            }
            
            .device {
                max-height: 350px;
                width: 100%;
                flex: 1;
            }
            
            .header h1 {
                font-size: 1.5rem;
            }
            
            .header p {
                font-size: 0.9rem;
            }
            
            .connection-arrows {
                display: none !important;
            }
        }

        @@media (max-width: 1024px) {
            .device {
                max-height: 320px;
            }
            
            .header h1 {
                font-size: 1.4rem;
            }
            
            .mobile-messages {
                min-height: 150px;
            }
            
            .processing-log {
                font-size: 0.75rem;
            }
            
            .bot-responses {
                font-size: 0.85rem;
            }
        }

        @@media (max-width: 768px) {
            .main-container {
                height: 100vh;
                overflow: hidden;
                display: flex;
                flex-direction: column;
            }
            
            .header {
                padding: 0.6rem 0.5rem;
                flex-shrink: 0;
            }
            
            .header h1 {
                font-size: 1.2rem;
                margin: 0;
            }
            
            .header p {
                font-size: 0.8rem;
                margin: 0.2rem 0 0 0;
            }
            
            .screens-container {
                padding: 0.4rem;
                gap: 0.4rem;
                flex: 1;
                min-height: 0;
                overflow: hidden;
                display: flex;
                flex-direction: column;
            }
            
            .device {
                max-height: none;
                height: auto;
                min-height: 0;
                flex: 1 1 33.333%;
                display: flex;
                flex-direction: column;
                overflow: hidden;
            }
            
            .device-header {
                padding: 0.4rem;
                font-size: 0.85rem;
                flex-shrink: 0;
            }
            
            .device-title {
                font-size: 0.75rem;
                gap: 0.2rem;
            }
            
            .device-title span {
                font-weight: bold;
            }
            
            .device-status {
                font-size: 0.65rem;
            }
            
            .status-dot {
                width: 6px;
                height: 6px;
            }
            
            .mobile-messages {
                padding: 0.4rem;
                min-height: 80px;
                flex: 1;
                overflow-y: auto;
            }
            
            .message {
                margin-bottom: 0.6rem;
            }
            
            .message-bubble {
                padding: 0.4rem 0.6rem;
                font-size: 0.75rem;
                max-width: 85%;
            }
            
            .message-info {
                font-size: 0.55rem;
                gap: 0.3rem;
            }
            
            .sentiment-badge {
                padding: 0.1rem 0.25rem;
                font-size: 0.5rem;
            }
            
            .mobile-input {
                padding: 0.35rem;
                flex-shrink: 0;
                min-height: 45px;
            }
            
            .input-group {
                gap: 0.4rem;
            }
            
            .form-control {
                padding: 0.35rem 0.5rem;
                font-size: 0.75rem;
            }
            
            .btn-send {
                width: 1.8rem;
                height: 1.8rem;
                font-size: 0.8rem;
                flex-shrink: 0;
            }
            
            .processing-log {
                padding: 0.4rem;
                font-size: 0.65rem;
                flex: 1;
                overflow-y: auto;
            }
            
            .log-entry {
                padding: 0.25rem;
                margin-bottom: 0.3rem;
                border-left-width: 2px;
            }
            
            .log-time {
                font-size: 0.55rem;
            }
            
            .log-message {
                font-size: 0.65rem;
                margin-top: 0.1rem;
            }
            
            .log-message i {
                font-size: 0.6rem;
            }
            
            .server-stats {
                padding: 0.35rem;
                grid-template-columns: repeat(4, 1fr);
                gap: 0.25rem;
                flex-shrink: 0;
            }
            
            .stat-item {
                padding: 0.25rem;
                font-size: 0.65rem;
            }
            
            .stat-value {
                font-size: 0.95rem;
                font-weight: bold;
            }
            
            .stat-name {
                font-size: 0.5rem;
                margin-top: 0.1rem;
            }
            
            .bot-responses {
                padding: 0.4rem;
                flex: 1;
                overflow-y: auto;
                font-size: 0.75rem;
            }
            
            .response-item {
                padding: 0.4rem;
                margin-bottom: 0.4rem;
                border-left-width: 3px;
            }
            
            .response-header {
                margin-bottom: 0.2rem;
                font-size: 0.7rem;
            }
            
            .response-sentiment {
                padding: 0.15rem 0.3rem;
                font-size: 0.55rem;
            }
            
            .response-time {
                font-size: 0.55rem;
            }
            
            .response-text {
                font-size: 0.7rem;
                margin: 0.25rem 0;
                line-height: 1.2;
            }
            
            .bot-thinking {
                padding: 0.4rem;
                font-size: 0.75rem;
                gap: 0.3rem;
            }
            
            .thinking-dots {
                gap: 0.15rem;
            }
            
            .thinking-dots .dot {
                width: 5px;
                height: 5px;
            }
            
            .current-sentiment {
                padding: 0.35rem;
                flex-shrink: 0;
                border-top-width: 1px;
            }
            
            .sentiment-display {
                font-size: 1.3rem;
                margin-bottom: 0.25rem;
            }
            
            .sentiment-label {
                font-size: 0.75rem;
            }
            
            .connection-arrows {
                display: none !important;
            }
            
            .welcome-message {
                padding: 1rem 0.5rem;
            }
            
            .welcome-message i {
                font-size: 1.3rem;
                margin-bottom: 0.5rem;
            }
            
            .welcome-message p {
                font-size: 0.8rem;
                margin: 0.5rem 0;
            }
            
            .welcome-message small {
                font-size: 0.65rem;
            }
        }

        @@media (max-width: 600px) {
            .header h1 {
                font-size: 1.1rem;
            }
            
            .header p {
                font-size: 0.75rem;
                margin: 0.15rem 0 0 0;
            }
            
            .header {
                padding: 0.4rem;
            }
            
            .device {
                flex: 1 1 33.333%;
            }
            
            .device-header {
                padding: 0.3rem;
                font-size: 0.8rem;
            }
            
            .device-title {
                font-size: 0.7rem;
            }
            
            .mobile-messages {
                padding: 0.3rem;
            }
            
            .message-bubble {
                padding: 0.35rem 0.5rem;
                font-size: 0.7rem;
            }
            
            .mobile-input {
                padding: 0.3rem;
                min-height: 42px;
            }
            
            .form-control {
                padding: 0.3rem 0.45rem;
                font-size: 0.7rem;
            }
            
            .btn-send {
                width: 1.6rem;
                height: 1.6rem;
                font-size: 0.75rem;
            }
            
            .processing-log {
                padding: 0.3rem;
                font-size: 0.6rem;
            }
            
            .server-stats {
                grid-template-columns: repeat(2, 1fr);
                padding: 0.3rem;
                gap: 0.2rem;
            }
            
            .stat-item {
                padding: 0.2rem;
                font-size: 0.6rem;
            }
            
            .stat-value {
                font-size: 0.85rem;
            }
            
            .stat-name {
                font-size: 0.45rem;
            }
            
            .bot-responses {
                padding: 0.3rem;
                font-size: 0.7rem;
            }
            
            .response-item {
                padding: 0.3rem;
                margin-bottom: 0.3rem;
            }
            
            .response-text {
                font-size: 0.65rem;
            }
            
            .sentiment-display {
                font-size: 1rem;
            }
        }

        @@media (max-width: 480px) {
            .main-container {
                height: 100vh;
                overflow: hidden;
            }
            
            .header h1 {
                font-size: 1rem;
            }
            
            .header p {
                font-size: 0.7rem;
            }
            
            .header {
                padding: 0.35rem 0.3rem;
            }
            
            .screens-container {
                padding: 0.3rem;
                gap: 0.3rem;
            }
            
            .device {
                flex: 1 1 33.333%;
                min-height: 0;
            }
            
            .device-header {
                padding: 0.25rem;
                font-size: 0.75rem;
            }
            
            .device-title {
                font-size: 0.65rem;
                gap: 0.15rem;
            }
            
            .device-status {
                font-size: 0.6rem;
            }
            
            .mobile-messages {
                padding: 0.25rem;
                min-height: 70px;
            }
            
            .message {
                margin-bottom: 0.4rem;
            }
            
            .message-bubble {
                padding: 0.3rem 0.4rem;
                font-size: 0.65rem;
                max-width: 90%;
            }
            
            .message-info {
                font-size: 0.5rem;
            }
            
            .sentiment-badge {
                padding: 0.08rem 0.2rem;
                font-size: 0.45rem;
            }
            
            .mobile-input {
                padding: 0.25rem;
                min-height: 38px;
            }
            
            .form-control {
                padding: 0.25rem 0.4rem;
                font-size: 0.65rem;
            }
            
            .btn-send {
                width: 1.5rem;
                height: 1.5rem;
                font-size: 0.7rem;
            }
            
            .processing-log {
                padding: 0.25rem;
                font-size: 0.55rem;
            }
            
            .log-entry {
                padding: 0.2rem;
                margin-bottom: 0.2rem;
            }
            
            .log-message {
                font-size: 0.6rem;
            }
            
            .server-stats {
                grid-template-columns: repeat(2, 1fr);
                padding: 0.25rem;
                gap: 0.15rem;
            }
            
            .stat-item {
                padding: 0.15rem;
                font-size: 0.55rem;
            }
            
            .stat-value {
                font-size: 0.8rem;
            }
            
            .stat-name {
                font-size: 0.4rem;
            }
            
            .bot-responses {
                padding: 0.25rem;
                font-size: 0.65rem;
            }
            
            .response-item {
                padding: 0.3rem;
                margin-bottom: 0.25rem;
            }
            
            .response-text {
                font-size: 0.6rem;
            }
            
            .sentiment-display {
                font-size: 0.9rem;
            }
            
            .sentiment-label {
                font-size: 0.65rem;
            }
            
            .welcome-message {
                padding: 0.6rem 0.3rem;
            }
            
            .welcome-message i {
                font-size: 1rem;
                margin-bottom: 0.3rem;
            }
            
            .welcome-message p {
                font-size: 0.7rem;
                margin: 0.3rem 0;
            }
            
            .welcome-message small {
                font-size: 0.6rem;
            }
        }

        @@media (max-width: 360px) {
            .header h1 {
                font-size: 0.9rem;
            }
            
            .header p {
                display: none;
            }
            
            .device-title span {
                display: none;
            }
            
            .device-status {
                font-size: 0.5rem;
            }
            
            .mobile-messages {
                font-size: 0.6rem;
            }
            
            .message-bubble {
                font-size: 0.55rem;
            }
            
            .form-control {
                font-size: 0.55rem;
            }
        }

        .current-sentiment {
            background: rgba(111, 66, 193, 0.1);
            padding: 0.75rem;
            border-top: 1px solid var(--border-color);
            text-align: center;
        }

        .sentiment-display {
            font-size: 2rem;
            margin-bottom: 0.5rem;
        }

        .sentiment-label {
            font-weight: bold;
            color: var(--text-primary);
        }

        .connection-arrows {
            position: absolute;
            top: 50%;
            font-size: 1.5rem;
            color: var(--accent);
            z-index: 10;
        }

        .arrow-mobile-server {
            left: -1rem;
        }

        .arrow-server-bot {
            right: -1rem;
        }

        ::-webkit-scrollbar {
            width: 6px;
        }

        ::-webkit-scrollbar-track {
            background: var(--bg-secondary);
        }

        ::-webkit-scrollbar-thumb {
            background: var(--border-color);
            border-radius: 3px;
        }

        .welcome-message {
            text-align: center;
            color: var(--text-secondary);
            padding: 2rem;
        }

        .welcome-message i {
            font-size: 2rem;
            color: var(--accent);
            margin-bottom: 1rem;
        }
    </style>
</head>
<body>
    <div class="main-container">
        <div class="header">
            <h1><i class="fas fa-network-wired me-2"></i>Three Screen Sentiment Analysis System</h1>
            <p>Real-time communication between Mobile, Server, and Bot devices</p>
        </div>

        <div class="screens-container">
            <!-- Mobile Device -->
            <div class="device mobile">
                <div class="device-header mobile-header">
                    <div class="device-title">
                        <i class="fas fa-mobile-alt"></i>
                        <span>Mobile Device</span>
                    </div>
                    <div class="device-status">
                        <div class="status-dot"></div>
                        <span>Connected</span>
                    </div>
                </div>

                <div class="mobile-messages" id="mobileMessages">
                    <div class="welcome-message">
                        <i class="fas fa-comments"></i>
                        <p>Type a message below to start chatting!</p>
                        <small>Your message will be analyzed on the server</small>
                    </div>
                </div>

                <div class="mobile-input">
                    <div class="input-group">
                        <input type="text" class="form-control" id="mobileInput" 
                               placeholder="Type your message..." maxlength="200">
                        <button class="btn-send" id="mobileSend" type="button">
                            <i class="fas fa-paper-plane"></i>
                        </button>
                    </div>
                </div>
            </div>

            <!-- Server Device -->
            <div class="device server">
                <div class="connection-arrows arrow-mobile-server">
                    <i class="fas fa-arrow-right"></i>
                </div>
                <div class="device-header server-header">
                    <div class="device-title">
                        <i class="fas fa-server"></i>
                        <span>Server Device</span>
                    </div>
                    <div class="device-status">
                        <div class="status-dot"></div>
                        <span>Processing</span>
                    </div>
                </div>

                <div class="server-content">
                    <div class="processing-log" id="serverLog">
                        <div class="log-entry">
                            <div class="log-time">[@DateTime.Now.ToString("HH:mm:ss")]</div>
                            <div class="log-message">
                                <i class="fas fa-check-circle me-1"></i>
                                Server initialized successfully
                            </div>
                        </div>
                        <div class="log-entry">
                            <div class="log-time">[@DateTime.Now.ToString("HH:mm:ss")]</div>
                            <div class="log-message">
                                <i class="fas fa-brain me-1"></i>
                                Sentiment analysis model loaded
                            </div>
                        </div>
                        <div class="log-entry">
                            <div class="log-time">[@DateTime.Now.ToString("HH:mm:ss")]</div>
                            <div class="log-message">
                                <i class="fas fa-link me-1"></i>
                                Waiting for messages from Mobile...
                            </div>
                        </div>
                    </div>

                    <div class="server-stats">
                        <div class="stat-item">
                            <div class="stat-value" id="totalProcessed">0</div>
                            <div class="stat-name">Total</div>
                        </div>
                        <div class="stat-item">
                            <div class="stat-value" id="positiveCount">0</div>
                            <div class="stat-name">Positive</div>
                        </div>
                        <div class="stat-item">
                            <div class="stat-value" id="negativeCount">0</div>
                            <div class="stat-name">Negative</div>
                        </div>
                        <div class="stat-item">
                            <div class="stat-value" id="neutralCount">0</div>
                            <div class="stat-name">Neutral</div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Bot Device -->
            <div class="device bot">
                <div class="connection-arrows arrow-server-bot">
                    <i class="fas fa-arrow-left"></i>
                </div>
                <div class="device-header bot-header">
                    <div class="device-title">
                        <i class="fas fa-robot"></i>
                        <span>Bot Device</span>
                    </div>
                    <div class="device-status">
                        <div class="status-dot"></div>
                        <span>AI Active</span>
                    </div>
                </div>

                <div class="bot-content">
                    <div class="bot-thinking" id="botThinking">
                        <div class="thinking-dots">
                            <div class="dot"></div>
                            <div class="dot"></div>
                            <div class="dot"></div>
                        </div>
                        <span>Bot is thinking...</span>
                    </div>

                    <div class="bot-responses" id="botResponses">
                        <div class="welcome-message">
                            <i class="fas fa-robot"></i>
                            <p>AI Bot Ready</p>
                            <small>Waiting for processed messages from Server...</small>
                        </div>
                    </div>

                    <div class="current-sentiment">
                        <div class="sentiment-display" id="currentSentimentEmoji">🤖</div>
                        <div class="sentiment-label" id="currentSentimentLabel">Ready</div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/microsoft-signalr/7.0.5/signalr.min.js"></script>
    <script>
        // Connection to SignalR hub
        const connection = new signalR.HubConnectionBuilder()
            .withUrl("/chatHub")
            .build();

        let messageId = 0;

        // Statistics
        let stats = {
            total: 0,
            positive: 0,
            negative: 0,
            neutral: 0
        };

        // Update server statistics
        function updateServerStats() {
            document.getElementById('totalProcessed').textContent = stats.total;
            document.getElementById('positiveCount').textContent = stats.positive;
            document.getElementById('negativeCount').textContent = stats.negative;
            document.getElementById('neutralCount').textContent = stats.neutral;
        }

        function getMessageRoleClass(sender) {
            return sender === 'Bot' ? 'bot' : 'user';
        }

        function getSentimentKey(sentiment) {
            const normalized = sentiment ? sentiment.toLowerCase() : '';
            if (normalized.includes('positive')) return 'positive';
            if (normalized.includes('negative')) return 'negative';
            if (normalized.includes('neutral')) return 'neutral';
            return 'neutral';
        }

        function getSentimentClass(sentiment) {
            const normalized = sentiment ? sentiment.toLowerCase() : 'neutral';
            if (normalized.includes('positive')) return 'sentiment-positive';
            if (normalized.includes('negative')) return 'sentiment-negative';
            if (normalized.includes('neutral')) return 'sentiment-neutral';
            return 'sentiment-neutral';
        }

        // Add message to mobile screen
        function addMobileMessage(message, isPlaceholder = false) {
            const mobileMessages = document.getElementById('mobileMessages');
            
            // Remove welcome message if it exists
            const welcomeMsg = mobileMessages.querySelector('.welcome-message');
            if (welcomeMsg) {
                welcomeMsg.remove();
            }

            const existingMessage = mobileMessages.querySelector(`[data-message-id="${message.id}"]`);
            const sentimentClass = getSentimentClass(message.sentiment);
            const sentimentText = isPlaceholder ? `${message.sentimentEmoji} Analyzing...` : `${message.sentimentEmoji} ${message.sentiment}`;

            if (existingMessage) {
                existingMessage.className = `message ${getMessageRoleClass(message.sender)}`;
                existingMessage.dataset.messageId = message.id;
                existingMessage.innerHTML = `
                    <div class="message-bubble">
                        <div>${message.text}</div>
                        <div class="message-info">
                            <span>${message.sender}</span>
                            <span>${new Date(message.timestamp).toLocaleTimeString()}</span>
                            <span class="sentiment-badge ${sentimentClass}">
                                ${sentimentText}
                            </span>
                        </div>
                    </div>
                `;
                mobileMessages.scrollTop = mobileMessages.scrollHeight;
                return;
            }
            
            const messageDiv = document.createElement('div');
            messageDiv.className = `message ${getMessageRoleClass(message.sender)}${isPlaceholder ? ' placeholder' : ''}`;
            messageDiv.dataset.messageId = message.id;
            
            messageDiv.innerHTML = `
                <div class="message-bubble">
                    <div>${message.text}</div>
                    <div class="message-info">
                        <span>${message.sender}</span>
                        <span>${new Date(message.timestamp).toLocaleTimeString()}</span>
                        <span class="sentiment-badge ${sentimentClass}">
                            ${sentimentText}
                        </span>
                    </div>
                </div>
            `;
            
            mobileMessages.appendChild(messageDiv);
            mobileMessages.scrollTop = mobileMessages.scrollHeight;
        }

        // Add log entry to server screen
        function addServerLog(message, type = 'info') {
            const serverLog = document.getElementById('serverLog');
            const entry = document.createElement('div');
            entry.className = `log-entry ${type}`;
            
            const time = new Date().toLocaleTimeString();
            let icon = '';
            let sentimentBadge = '';
            
            if (type === 'processing') {
                icon = '<i class="fas fa-spinner fa-spin me-1"></i>';
            } else if (type === 'positive') {
                icon = '<i class="fas fa-smile me-1"></i>';
                sentimentBadge = `<span class="sentiment-badge sentiment-positive">${message.sentimentEmoji} ${message.sentiment}</span>`;
            } else if (type === 'negative') {
                icon = '<i class="fas fa-frown me-1"></i>';
                sentimentBadge = `<span class="sentiment-badge sentiment-negative">${message.sentimentEmoji} ${message.sentiment}</span>`;
            } else if (type === 'neutral') {
                icon = '<i class="fas fa-meh me-1"></i>';
                sentimentBadge = `<span class="sentiment-badge sentiment-neutral">${message.sentimentEmoji} ${message.sentiment}</span>`;
            } else {
                icon = '<i class="fas fa-info-circle me-1"></i>';
            }
            
            entry.innerHTML = `
                <div class="log-time">[${time}]</div>
                <div class="log-message">
                    ${icon}
                    ${message.text || message}
                    ${sentimentBadge}
                </div>
            `;
            
            serverLog.appendChild(entry);
            serverLog.scrollTop = serverLog.scrollHeight;
            
            // Keep only last 20 log entries
            while (serverLog.children.length > 20) {
                serverLog.removeChild(serverLog.firstChild);
            }
        }

        // Add response to bot screen
        function addBotResponse(message) {
            const botResponses = document.getElementById('botResponses');
            
            // Remove welcome message if it exists
            const welcomeMsg = botResponses.querySelector('.welcome-message');
            if (welcomeMsg) {
                welcomeMsg.remove();
            }
            
            const sentimentKey = getSentimentKey(message.sentiment);
            const sentimentClass = getSentimentClass(message.sentiment);
            
            const responseDiv = document.createElement('div');
            responseDiv.className = `response-item ${sentimentKey}`;
            
            responseDiv.innerHTML = `
                <div class="response-header">
                    <span class="response-sentiment ${sentimentClass}">
                        ${message.sentimentEmoji} ${message.sentiment}
                    </span>
                    <span class="response-time">${new Date(message.timestamp).toLocaleTimeString()}</span>
                </div>
                <div class="response-text">${message.text}</div>
            `;
            
            botResponses.appendChild(responseDiv);
            botResponses.scrollTop = botResponses.scrollHeight;
            
            // Update current sentiment display
            document.getElementById('currentSentimentEmoji').textContent = message.sentimentEmoji;
            document.getElementById('currentSentimentLabel').textContent = message.sentiment;
        }

        // Handle incoming messages
        connection.on("ReceiveMessage", (message) => {
            console.log('Received message:', message);
            
            if (message.sender === 'Mobile User') {
                // Add to mobile screen immediately
                addMobileMessage(message);
                
                // Show processing on server
                addServerLog({
                    text: `📱 Received from Mobile: "${message.text}"`
                }, 'processing');
                
                // Show bot thinking
                document.getElementById('botThinking').classList.add('active');
                
                // Simulate server processing
                setTimeout(() => {
                    // Update server stats
                    stats.total++;
                    const sentimentKey = getSentimentKey(message.sentiment);
                    if (stats.hasOwnProperty(sentimentKey)) {
                        stats[sentimentKey]++;
                    }
                    updateServerStats();
                    
                    // Show sentiment analysis complete
                    addServerLog(message, sentimentKey);
                    
                    // Update bot current sentiment
                    document.getElementById('currentSentimentEmoji').textContent = message.sentimentEmoji;
                    document.getElementById('currentSentimentLabel').textContent = message.sentiment;
                }, 1000);
                
            } else if (message.sender === 'Bot') {
                // Hide bot thinking
                document.getElementById('botThinking').classList.remove('active');
                
                // Add bot response to mobile screen
                addMobileMessage(message);
                
                // Add bot response to bot screen
                addBotResponse(message);
                
                // Log bot response on server
                addServerLog({
                    text: `🤖 Bot Response: "${message.text}" (${message.sentiment})`
                }, 'info');
            }
        });

        // Send message from mobile
        async function sendMobileMessage() {
            const input = document.getElementById('mobileInput');
            const text = input.value.trim();
            
            if (!text) return;
            
            const message = {
                id: (++messageId).toString(),
                text: text,
                sender: 'Mobile User',
                timestamp: new Date().toISOString(),
                sentiment: 'Processing...',
                sentimentEmoji: '⏳',
                confidence: 0
            };
            
            // Add placeholder message while waiting for sentiment analysis
            addMobileMessage(message, true);
            input.value = '';
            
            try {
                const response = await fetch('/api/chat/message', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify(message)
                });
                
                if (!response.ok) {
                    throw new Error('Failed to send message');
                }
            } catch (error) {
                console.error('Error sending message:', error);
                alert('Failed to send message. Please check connection.');
            }
        }

        // Event listeners
        document.getElementById('mobileSend').addEventListener('click', sendMobileMessage);
        document.getElementById('mobileInput').addEventListener('keypress', (e) => {
            if (e.key === 'Enter') {
                sendMobileMessage();
            }
        });

        // Show bot thinking when mobile user sends message
        document.getElementById('mobileInput').addEventListener('input', () => {
            if (document.getElementById('mobileInput').value.trim()) {
                // Optional: Show thinking indicator
            }
        });

        // Start connection
        connection.start()
            .then(() => {
                console.log('Connected to SignalR hub');
                addServerLog('Connected to SignalR hub successfully');
            })
            .catch(err => {
                console.error('SignalR connection error:', err);
                addServerLog('SignalR connection error: ' + err.message, 'error');
            });
    </script>
</body>
</html>
